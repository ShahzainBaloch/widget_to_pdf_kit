name: widget_to_pdf_kit_example
description: Demonstrates how to use widget_to_pdf_kit.
publish_to: 'none'
version: 1.0.0

environment:
  sdk: ">=3.6.0 <4.0.0"
  flutter: ">=3.27.0"

dependencies:
  flutter:
    sdk: flutter
  widget_to_pdf_kit:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
